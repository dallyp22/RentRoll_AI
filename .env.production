# Production Environment Variables for RentRoll AI
# Copy these to your deployment platform (Railway/Render)

# BigQuery Configuration
BQ_PROJECT=rentroll-ai
BIGQUERY_PROJECT=rentroll-ai

# Google Cloud Credentials (set as environment variable in deployment platform)
# GOOGLE_APPLICATION_CREDENTIALS will be set via Railway/Render dashboard

# OpenAI API Key (get from OpenAI dashboard)
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
PORT=4000
NODE_ENV=production

# Frontend URL (will be your Vercel URL)
FRONTEND_URL=https://your-app.vercel.app 